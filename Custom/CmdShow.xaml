<UserControl x:Class="CmdControl.Custom.CmdShow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CmdControl.Custom"
             mc:Ignorable="d" 
             d:DesignHeight="553" d:DesignWidth="855">
    <Grid>
        <Rectangle Fill="#FFF4F4F5" Stroke="#FF5ABED6" StrokeThickness="2"/>
        <GroupBox Header="日志" Margin="4,4,4,100">
            <Grid VerticalAlignment="Top" HorizontalAlignment="Left">
                <ScrollViewer Style="{StaticResource ScrollViewer}">
                    <TextBlock Text="TextBlock" TextWrapping="Wrap" Margin="0,0,-843,-424"/>
                </ScrollViewer>
                <Button Content="清空" Margin="0,-25,-767,0" HorizontalAlignment="Right" VerticalAlignment="Top" Width="60"/>
                <Button Content="保存" Margin="0,-25,-697,0" HorizontalAlignment="Right" VerticalAlignment="Top" Width="60"/>

            </Grid>
        </GroupBox>

    </Grid>
</UserControl>
